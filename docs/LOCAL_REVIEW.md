# Local rubric review

Reviewed 20 September 2026 against the Problem 2 brief and free evaluator prompt. This is a source-and-evidence review, not an official scored attempt.

| Technical criterion                       | Weight | Evidence and finding                                                                                                                                                                                                                                                                                      |
| ----------------------------------------- | -----: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Check upload capability before writes     |     12 | `publish.ts` reads current connection information before every raw object upload and immediately before the signed feed update. Tests prove no writes when signed out/unavailable, and stop later writes after capability loss or account changes.                                                        |
| Default path without a personal stamp     |      6 | A fresh test identity with no personal drive saved two records and a photo through the configured sponsored gateway, with the local Bee and tunnel stopped.                                                                                                                                               |
| Format and version in actual stored bytes |     14 | Exact network objects in `evidence/objects/` carry `format` and `version`. The manifest and independent verifier record their sizes and checksums.                                                                                                                                                        |
| Independent reader                        |     14 | Separate app, entrypoint, package, parser, network module, build and deployment. Import-boundary check and isolated CI build pass. A clean incognito session retrieved both records without the writer or identity session.                                                                               |
| Correct matching endpoint family          |     10 | Raw content is uploaded with `uploadData` and retrieved through `/bytes`. Real record/photo readback and cross-app retrieval passed.                                                                                                                                                                      |
| No gateway pin/tag options                |      6 | The application passes only `{ encrypt: false }` for public objects and feed index/timestamp settings for the feed. Tests assert the object upload option allowlist; no application pin/tag/ACT options are present.                                                                                      |
| Specific visible failures                 |     10 | Capability, identity change, conflict, readback integrity, unavailable objects, unsupported versions and timeout cases produce distinct messages. Negative automated checks pass. Interactive invalid-link and wrong-format reader errors were also verified; capability-loss checks use automated tests. |
| No tracked credentials                    |      8 | Source scan and CI pass; local account material is ignored and owner-readable only. Public evidence contains locators and synthetic records, not authentication material.                                                                                                                                 |

The 20-point qualitative criterion is supported by a usable birding workflow, meaningful date/time/location precision, photo attribution, public-data notices, a distinct independent reader, documented schemas and retrieval rules, and real network-backed evidence. The same notebook link discovers later sightings without exporting or importing a file.

Follow-up review completed the 375-pixel mobile inspection, fixed reader overflow and stale species names, verified sign-out/storage-clear/sign-back-in recovery, and demonstrated two specific reader errors. See [the Loops evaluator report](LOOPS_EVALUATION.md). The current [repository review](REPOSITORY_REVIEW.md) also documents retry-safety fixes and 29 passing tests. Source, test cases and committed evidence form the review path; no official score is claimed.

The [format contract](../format/README.md) defines the public record encoding, app-derived publishing identity, one-active-writer procedure and object validation rules.
